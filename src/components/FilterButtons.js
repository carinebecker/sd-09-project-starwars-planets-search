import React from 'react';

const FilterButtons = () => {
  const string = 'Esse é o componente FilterButtons';

  return (
    <div>
      <p>{ string }</p>
    </div>
  );
};

export default FilterButtons;
