import React from 'react';
// import '../css/Loading';

export default class Loading extends React.Component {
  render() {
    return (
      <section>
        Loading...
      </section>
    );
  }
}
